# Отчёт об инспектировании кода

**Проект:** Информационная система учёта заявок пользователей  
**Этап:** Инспектирование кода после интеграции и тестирования  
**Дата:** 03.07.2026

---

## 1. Краткое описание архитектуры проекта

Приложение построено по **слоистой архитектуре** (layered architecture) и предназначено для учёта пользователей и их заявок в консольном режиме.

```text
main.py
   └── ApplicationService          ← composition root (сборка зависимостей)
           ├── UserRepository
           ├── TicketRepository
           ├── UserService
           └── TicketService ──→ UserService   ← интеграция модулей
   └── ConsoleInterface            ← ввод/вывод, без бизнес-логики
```

| Слой | Назначение | Ключевые компоненты |
|------|------------|---------------------|
| **models** | Доменные сущности | `User`, `Ticket`, `TicketStatus` |
| **repositories** | Хранение данных в памяти | `UserRepository`, `TicketRepository` |
| **services** | Бизнес-логика | `UserService`, `TicketService`, `ApplicationService` |
| **interface** | Представление | `ConsoleInterface` |
| **exceptions** | Обработка ошибок | `AppError`, `ValidationError`, `NotFoundError`, `DuplicateError` |
| **utils** | Вспомогательные функции | `validators` |

**Поток данных при создании заявки:**

1. `ConsoleInterface` вызывает `TicketService.create_ticket()`.
2. `TicketService` проверяет заголовок и описание (`validators.is_not_empty`).
3. `TicketService` вызывает `UserService.get_user()` — проверка существования пользователя.
4. При успехе `TicketRepository.add()` сохраняет заявку.
5. При ошибке выбрасывается типизированное исключение (`ValidationError` / `NotFoundError`), перехватываемое в `ConsoleInterface`.

---

## 2. UML-диаграмма

Актуальная диаграмма классов находится в файле [`docs/uml.puml`](uml.puml).

Для просмотра используйте PlantUML или расширение VS Code/Cursor для `.puml` файлов.

**Основные связи на диаграмме:**

- `ApplicationService` создаёт репозитории и сервисы.
- `TicketService` использует `UserService` для проверки пользователя при создании заявки.
- `ConsoleInterface` обращается к сервисам и обрабатывает `AppError`.
- `Ticket` связан с `User` через поле `created_by_user_id`.

---

## 3. Результаты инспектирования

### 3.1. Понятность имён классов, методов и переменных

| Оценка | **Хорошо** |
|--------|------------|

**Сильные стороны:**
- Имена классов отражают роль: `UserService`, `TicketRepository`, `ConsoleInterface`.
- Методы названы по действию: `create_user`, `create_ticket`, `list_active_users`, `change_status`.
- Поля моделей понятны: `full_name`, `created_by_user_id`, `is_active`.
- Приватные атрибуты сервисов помечены `_` (`_user_repository`, `_ticket_service`).

**Замечания:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 1 | В коде сущность «Заявка» названа `Ticket`, в документации — «Заявки». Допустимо для учебного проекта, но при расширении стоит унифицировать терминологию. | Низкая | Принято как есть |

---

### 3.2. Разделение ответственности (SRP)

| Оценка | **Хорошо** |
|--------|------------|

| Компонент | Ответственность | Соблюдение |
|-----------|-----------------|------------|
| `UserRepository` / `TicketRepository` | Только CRUD в памяти | ✓ |
| `UserService` / `TicketService` | Бизнес-правила и валидация | ✓ |
| `ApplicationService` | Сборка зависимостей (DI) | ✓ |
| `ConsoleInterface` | Ввод/вывод, демо-сценарий | ✓ |
| `validators` | Переиспользуемые проверки | ✓ |

**Замечания:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 2 | `list_user_tickets()` не проверяет существование пользователя — метод делегирует фильтрацию репозиторию без обращения к `UserService`. Для текущего объёма допустимо (вернёт пустой список). | Низкая | Зафиксировано как рекомендация |
| 3 | `ApplicationService` использует публичные атрибуты (`user_service`), тогда как сервисы — приватные (`_user_repository`). Стиль немного различается. | Низкая | Принято как есть |

---

### 3.3. Корректность обработки исключений

| Оценка | **Хорошо** |
|--------|------------|

**Реализовано:**
- Иерархия исключений от `AppError` — единая точка перехвата в UI.
- Сервисы не возвращают `None` наружу при ошибках — преобразуют в `NotFoundError`.
- Сообщения об ошибках информативны: `"Пользователь с ID {id} не найден"`.
- `ConsoleInterface.run_demo()` перехватывает `AppError` — приложение не падает при ошибках интеграции.

**Замечания:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 4 | В `ConsoleInterface` все ошибки выводятся одинаково, без разделения по типу (`ValidationError` / `NotFoundError`). Для демо-приложения достаточно. | Низкая | Принято как есть |

---

### 3.4. Отсутствие дублирования логики

| Оценка | **Удовлетворительно** |
|--------|------------------------|

**Сильные стороны:**
- Валидация email и пустых строк вынесена в `validators.py`.
- Проверка «объект не найден» следует единому шаблону во всех сервисах.

**Замечания и исправления:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 5 | `UserService` проверял пустое ФИО через `if not full_name`, а `TicketService` — через `is_not_empty()`. Разные подходы к одной задаче. | Средняя | **Исправлено:** `UserService` переведён на `is_not_empty()` |
| 6 | Шаблон «получить из репозитория → если None → NotFoundError» повторяется в 4 методах. Для текущего масштаба приемлемо; при росте проекта можно вынести в базовый класс. | Низкая | Принято как есть |

---

### 3.5. Соответствие PEP 8 и стилю Python

| Оценка | **Хорошо** |
|--------|------------|

**Проверено:**
- Аннотации типов для параметров и возвращаемых значений.
- Docstring у классов сервисов, репозиториев и интерфейса.
- Импорты сгруппированы (стандартная библиотека → локальные модули).
- Использование `dataclass` для моделей.
- Линтер `ruff check src tests` — **замечаний нет**.

**Замечания:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 7 | Строка фильтрации в `TicketRepository.list_by_user_id` длиннее 79 символов (PEP 8 рекомендует 79). Читаемость не страдает. | Низкая | Принято как есть |

---

### 3.6. Читаемость и сопровождаемость

| Оценка | **Хорошо** |
|--------|------------|

**Сильные стороны:**
- Плоская структура каталогов, легко найти нужный слой.
- `ApplicationService` централизует создание зависимостей — изменение связей в одном месте.
- 17 автотестов покрывают модульные и интеграционные сценарии.
- UML-диаграмма актуализирована после интеграции.

**Замечания:**

| № | Замечание | Критичность | Статус |
|---|-----------|-------------|--------|
| 8 | UML-диаграмма содержала пометку «планируется» для связи `TicketService → UserService`. | Средняя | **Исправлено:** диаграмма обновлена |

---

## 4. Сводная таблица замечаний

| № | Категория | Замечание | Критичность | Действие |
|---|-----------|-----------|-------------|----------|
| 1 | Именование | Ticket vs «Заявка» | Низкая | Без изменений |
| 2 | SRP | `list_user_tickets` без проверки User | Низкая | Рекомендация |
| 3 | Стиль | Публичные атрибуты ApplicationService | Низкая | Без изменений |
| 4 | Исключения | Единый вывод ошибок в UI | Низкая | Без изменений |
| 5 | Дублирование | Разные проверки пустых строк | Средняя | **Исправлено** |
| 6 | Дублирование | Повтор шаблона NotFoundError | Низкая | Без изменений |
| 7 | PEP 8 | Длина строки в list comprehension | Низкая | Без изменений |
| 8 | Документация | Устаревшая UML-диаграмма | Средняя | **Исправлено** |

**Итого:** 8 замечаний — 2 исправлено, 6 принято / зафиксировано как рекомендации.

---

## 5. Описание внесённых исправлений (все этапы)

### Этап 1. Интеграция модулей

| Файл | Исправление |
|------|-------------|
| `ticket_service.py` | Добавлена зависимость от `UserService`; вызов `get_user()` перед созданием заявки |
| `application_service.py` | Передача `user_service` в конструктор `TicketService` |
| `ticket_repository.py` | Исправлена фильтрация `list_by_user_id`; исправлен `update_status` |
| `user_service.py` | Исправлен `list_active_users` — фильтрация по `is_active` |
| `validators.py` | Добавлена проверка пустой локальной части email |

### Этап 2. Тестовые сценарии

| Файл | Исправление |
|------|-------------|
| `tests/test_integration.py` | Добавлено 7 интеграционных тестов |
| `tests/test_ticket_service_expected_behavior.py` | Обновлена инициализация `TicketService` с `UserService` |
| `tests/test_additional_expected_behavior.py` | Обновлена инициализация для интеграции |

### Этап 3. Инспектирование (текущий)

| Файл | Исправление |
|------|-------------|
| `user_service.py` | Унифицирована проверка пустого ФИО через `is_not_empty()` |
| `docs/uml.puml` | Обновлена связь `TicketService → UserService`, удалена пометка «планируется» |

---

## 6. Проверка после инспектирования

```bash
cd requests
python -m ruff check src tests    # 0 замечаний
python -m pytest -v               # 17 passed
cd src && python -m app.main      # демо работает
```

| Проверка | Результат |
|----------|-----------|
| Линтер (ruff) | ✓ Без замечаний |
| Автотесты | ✓ 17/17 |
| Консольное приложение | ✓ Работает |

---

## 7. Заключение

Код после интеграции модулей «Пользователи» и «Заявки» соответствует принципам слоистой архитектуры и разделения ответственности. Именование понятное, исключения обрабатываются централизованно, стиль Python соблюдён. Выявленные замечания носят преимущественно рекомендательный характер; критичных дефектов не обнаружено. Проект готов к демонстрации и дальнейшему развитию.
